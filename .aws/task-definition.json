{
  "family": "memory-lens-backend-task",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::<AWS_ACCOUNT_ID>:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::<AWS_ACCOUNT_ID>:role/memory-lens-task-role",
  "containerDefinitions": [
    {
      "name": "api",
      "image": "public.ecr.aws/docker/library/python:3.11-slim",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 8000,
          "hostPort": 8000,
          "protocol": "tcp"
        }
      ],
      "environment": [
        {
          "name": "APP_ENV",
          "value": "production"
        },
        {
          "name": "DATA_DIR",
          "value": "/app/data"
        },
        {
          "name": "SQLITE_PATH",
          "value": "/app/data/memory_lens.db"
        },
        {
          "name": "ALLOWED_ORIGINS",
          "value": "http://localhost,http://localhost:3000,http://127.0.0.1,http://127.0.0.1:3000"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/memory-lens-backend-service",
          "awslogs-region": "ap-south-1",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
